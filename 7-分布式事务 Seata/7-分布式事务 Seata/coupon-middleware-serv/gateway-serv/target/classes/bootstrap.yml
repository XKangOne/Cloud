spring:
  application:
    name: coupon-gateway-serv
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        # 开启监听和自动刷新
        refresh-enabled: true

